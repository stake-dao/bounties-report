# ENG-2178: historical Curve recovery

Status: direct funding selected; no OTC deposit/fill. The recovery is inactive (`funding: null`) in [the recovery ledger](../data/sdtokens-recoveries.json).

- Three Arbitrum claims total `1202.234621 USDC`. Their receipts, campaign IDs, gauge, recipient and historical proposals are verified.
- The recipient held `0` before those claims, `1202.234621 USDC` immediately before withdrawal and `0` afterward. The reviewed withdrawal was its only outgoing USDC transfer over that interval.
- The bridge origin transferred `1201.032387 USDC`; the destination delivered `0.436072337174991503 WETH` to `0x8898502BA35AB64b3562aBC509Befb7Eb178D4df`. Both receipts are verified; the origin/destination pairing was supplied by the user.
- `1.202234 USDC` remains outside this recovery. The ledger reserves only the bridged portion of each source claim, leaving the rest available for a separate recovery.

| Source epoch | Original USDC | Reserved for this recovery |
| --- | --- | --- |
| 1788393600 | 481.918878 | 481.436959 |
| 1788998400 | 481.671424 | 481.189753 |
| 1789603200 | 238.644319 | 238.405675 |

The actual sdCRV received by the distributor will be split using those reserved amounts and the original proposals' voter, auto-voter and delegation rules. The next Merkle adds these wallet credits to normal weekly rewards and unclaimed carry. Published historical trees stay unchanged.

The [generic recovery module](sdtokens-recoveries.md) supports historical votes and explicit wallet amounts for sdCRV/sdFXN. Ordinary reports are unchanged. Direct funding is verified separately and deducted from the amount still needing funding and from the surplus calculation.

Read-only preview, using the current WETH amount as allocation units rather than a payable sdCRV amount:

```sh
pnpm tsx script/sdTkns/recoveries.ts --preview ENG-2178 436072337174991503 /tmp/eng-2178-preview.json
```

Validation:

- 418 existing unit tests passed; no tests added to the repository.
- Receipt-verified preview: 316 positive recipients, exact conservation of `436072337174991503` allocation units.
- 49 isolated checks cover explicit wallet amounts, source limits, duplicate funding, replay, wrong token/recipient/amount, reverted or unconfirmed receipts, funding caps and both attribution destinations.
- Repository-wide TypeScript checks retain existing failures; no diagnostics in the changed recovery, Merkle or verification files.
- Real swap/funding and full post-freeze distribution remain pending.

Before funding:

1. Review the direct-funding change and choose a target period that has not generated recovery evidence or a verified distribution.
2. Prepare and simulate the wallet conversion of exactly `0.436072337174991503 WETH` into sdCRV, then the direct sdCRV transfer to `0x03E34b085C52985F6a5D27243F20C84bDdc01Db4`.
3. After execution, add the conversion receipt to `trail`; set `funding.period`, `transaction`, `logIndex`, `from` and the exact received `amount`. Wait for 12 confirmations.
4. Run generation, R1–R5, post-freeze reconstruction and root checks. Review the archived wallet credits before publishing the new root.
