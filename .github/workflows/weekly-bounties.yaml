name: "[Bounties] Process - Weekly Claims"

on:
  workflow_call:
    inputs:
      bountyType:
        type: string
        required: true
      pastWeek:
        type: string
        required: false
        default: "0"
  workflow_dispatch:
    inputs:
      bountyType:
        description: "Type of bounty to process"
        required: true
        type: choice
        options:
          - votemarket
          - votemarket-v2
          - warden
          - hiddenhand
          - convex
          - convex-v2
          - votium
          - spectra
      pastWeek:
        description: "Number of weeks in the past (0 for current week)"
        required: true
        default: "0"
jobs:
  process-bounties:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Configure git and pull latest changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git pull origin main --rebase

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10.13"

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "20.5.1"

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9.12.3

      - name: Install node dependencies
        run: pnpm install

      - name: Run generate bounties script
        if: github.event_name != 'schedule'
        run: |
          if [[ "${{ inputs.bountyType || github.event.inputs.bountyType }}" == "votium" ]]; then
            pnpm tsx script/vlCVX/claims/generateConvexVotium.ts ${{ inputs.pastWeek || github.event.inputs.pastWeek }}
            # Run indexer after votium processing
            make -f automation/indexer.mk get-delegators
            make -f automation/indexer.mk commit-and-push
          else
            make -f automation/weekly-bounties.mk run-${{ inputs.bountyType || github.event.inputs.bountyType }} PAST_WEEK=${{ inputs.pastWeek || github.event.inputs.pastWeek }}
          fi
        env:
          EXPLORER_KEY: ${{ secrets.ETHERSCAN_TOKEN}}
          WEB3_ALCHEMY_API_KEY: ${{ secrets.WEB3_ALCHEMY_API_KEY }}
          TELEGRAM_VERIF_API_KEY: ${{ secrets.TELEGRAM_VERIF_API_KEY }}
          TELEGRAM_VERIF_CHAT_ID: ${{ secrets.TELEGRAM_VERIF_CHAT_ID }}

      - name: Run generate bounties script (all on schedule)
        if: github.event_name == 'schedule'
        run: |
          # Calculate current period
          WEEK=604800
          CURRENT_TIMESTAMP=$(date +%s)
          CURRENT_PERIOD=$(( CURRENT_TIMESTAMP / WEEK * WEEK ))
          BASE_DIR="weekly-bounties/$CURRENT_PERIOD"

          # Check and run each protocol if needed
          if [ ! -f "$BASE_DIR/votemarket/claimed_bounties.json" ]; then
            make -f automation/weekly-bounties.mk run-votemarket PAST_WEEK=0
          fi

          if [ ! -f "$BASE_DIR/votemarket-v2/claimed_bounties.json" ]; then
            make -f automation/weekly-bounties.mk run-votemarket-v2 PAST_WEEK=0
          fi

          if [ ! -f "$BASE_DIR/warden/claimed_bounties.json" ]; then
            make -f automation/weekly-bounties.mk run-warden PAST_WEEK=0
          fi

          if [ ! -f "$BASE_DIR/hiddenhand/claimed_bounties.json" ]; then
            make -f automation/weekly-bounties.mk run-hiddenhand PAST_WEEK=0
          fi

          if [ ! -f "$BASE_DIR/votemarket/claimed_bounties_convex.json" ]; then
            make -f automation/weekly-bounties.mk run-convex PAST_WEEK=0
          fi

          if [ ! -f "$BASE_DIR/votemarket-v2/claimed_bounties_convex.json" ]; then
            make -f automation/weekly-bounties.mk run-convex-v2 PAST_WEEK=0
          fi

          if [ ! -f "$BASE_DIR/spectra/claimed_bounties.json" ]; then
            make -f automation/weekly-bounties.mk run-spectra PAST_WEEK=0
          fi

          if [ ! -f "$BASE_DIR/votium/claimed_bounties_convex.json" ]; then
            pnpm tsx script/vlCVX/claims/generateConvexVotium.ts 0
          fi
        env:
          EXPLORER_KEY: ${{ secrets.ETHERSCAN_TOKEN}}
          WEB3_ALCHEMY_API_KEY: ${{ secrets.WEB3_ALCHEMY_API_KEY }}
          TELEGRAM_VERIF_API_KEY: ${{ secrets.TELEGRAM_VERIF_API_KEY }}
          TELEGRAM_VERIF_CHAT_ID: ${{ secrets.TELEGRAM_VERIF_CHAT_ID }}

      - uses: stefanzweifel/git-auto-commit-action@v6
        with:
          commit_message: "chore: Update ${{ inputs.bountyType || github.event.inputs.bountyType || 'weekly' }} bounties"
          commit_user_name: github-actions[bot]
          commit_user_email: 41898282+github-actions[bot]@users.noreply.github.com
          commit_author: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
          pull_options: '--rebase --autostash'

      - name: Trigger all merkle generation workflows (scheduled run)
        if: github.event_name == 'schedule'
        run: |
          echo "Triggering all merkle generation workflows for scheduled run..."

          # Trigger SD tokens merkle generation (Spectra + sdFXS)
          curl -X POST \
            -H "Accept: application/vnd.github.v3+json" \
            -H "Authorization: token ${{ secrets.GIT_ACCESS_TOKEN }}" \
            https://api.github.com/repos/${{ github.repository }}/dispatches \
            -d '{"event_type": "generate-all-sd-merkles"}'

          echo "Triggered SD tokens merkle generation"

          # Trigger vlCVX merkle generation (non-delegators)
          curl -X POST \
            -H "Accept: application/vnd.github.v3+json" \
            -H "Authorization: token ${{ secrets.GIT_ACCESS_TOKEN }}" \
            https://api.github.com/repos/${{ github.repository }}/dispatches \
            -d '{"event_type": "generate-vlcvx-merkles", "client_payload": {"type": "non-delegators"}}'

          echo "Triggered vlCVX merkle generation"

      - name: Trigger merkle generation workflows
        if: |
          (inputs.bountyType == 'spectra' || github.event.inputs.bountyType == 'spectra')
        run: |
          # Trigger Spectra merkle generation
          curl -X POST \
            -H "Accept: application/vnd.github.v3+json" \
            -H "Authorization: token ${{ secrets.GIT_ACCESS_TOKEN }}" \
            https://api.github.com/repos/${{ github.repository }}/dispatches \
            -d '{"event_type": "generate-spectra-merkle"}'

          echo "Triggered Spectra merkle generation workflow"

      - name: Trigger vlCVX merkle generation workflows
        if: |
          (inputs.bountyType == 'votium' || github.event.inputs.bountyType == 'votium')
        run: |
          # Trigger vlCVX merkle generation
          curl -X POST \
            -H "Accept: application/vnd.github.v3+json" \
            -H "Authorization: token ${{ secrets.GIT_ACCESS_TOKEN }}" \
            https://api.github.com/repos/${{ github.repository }}/dispatches \
            -d '{"event_type": "generate-vlcvx-merkles", "client_payload": {"type": "non-delegators"}}'

          echo "Triggered vlCVX merkle generation workflow"

      - name: Wait for triggered workflows to complete
        if: |
          github.event_name == 'schedule' ||
          (inputs.bountyType == 'spectra' || github.event.inputs.bountyType == 'spectra' ||
           inputs.bountyType == 'votium' || github.event.inputs.bountyType == 'votium')
        run: |
          echo "Waiting for triggered workflows to complete..."
          sleep 60  # Give workflows time to start

          # Define wait and interval constants (30 minutes max wait, 30 second intervals)
          MAX_WAIT_SECONDS=1800
          CHECK_INTERVAL_SECONDS=30

          # Function to check if a workflow is running
          check_workflow_status() {
            local workflow_name="$1"
            local max_wait=$MAX_WAIT_SECONDS
            local waited=0
            local check_interval=$CHECK_INTERVAL_SECONDS

            while [ $waited -lt $max_wait ]; do
              # Get the latest workflow run
              latest_run=$(curl -f -s \
                -H "Accept: application/vnd.github.v3+json" \
                -H "Authorization: token ${{ secrets.GIT_ACCESS_TOKEN }}" \
                "https://api.github.com/repos/${{ github.repository }}/actions/workflows/${workflow_name}/runs?per_page=1&branch=main") || {
                echo "Failed to fetch workflow status for $workflow_name"
                return 1
              }

              run_count=$(echo "$latest_run" | jq '.workflow_runs | length')
              if [ "$run_count" -eq 0 ]; then
                echo "✗ No workflow runs found for $workflow_name"
                return 1
              fi
              status=$(echo "$latest_run" | jq -r '.workflow_runs[0].status')
              conclusion=$(echo "$latest_run" | jq -r '.workflow_runs[0].conclusion')

              if [ "$status" = "completed" ]; then
                if [ "$conclusion" = "success" ]; then
                  echo "✓ Workflow $workflow_name completed successfully"
                  return 0
                else
                  echo "✗ Workflow $workflow_name failed with conclusion: $conclusion"
                  return 1
                fi
              fi

              echo "Workflow $workflow_name still running... (waited ${waited}s)"
              sleep $check_interval
              waited=$((waited + check_interval))
            done

            echo "✗ Timeout waiting for workflow $workflow_name"
            return 1
          }

          # Check workflows based on event type and bounty type
          if [ "${{ github.event_name }}" = "schedule" ]; then
            # For scheduled runs, wait for both merkle workflows
            check_workflow_status "sdtkns-merkles.yaml"
            check_workflow_status "vlcvx-merkles.yaml"
          else
            # For manual runs, check based on bounty type
            if [[ "${{ inputs.bountyType || github.event.inputs.bountyType }}" == "spectra" ]]; then
              check_workflow_status "sdtkns-merkles.yaml"
            fi

            if [[ "${{ inputs.bountyType || github.event.inputs.bountyType }}" == "votium" ]]; then
              check_workflow_status "vlcvx-merkles.yaml"
            fi
          fi

      - name: Send error notification to Telegram
        if: failure()
        uses: appleboy/telegram-action@v1.0.1
        with:
          to: -1002354704686
          token: ${{ secrets.TG_API_KEY_BOT_ERROR }}
          format: html
          message: |
            ❌ <b>Workflow failed</b>
            • <b>Workflow:</b> ${{ github.workflow }}
            • <b>Job:</b> process-bounties
            • <b>Bounty Type:</b> ${{ inputs.bountyType || github.event.inputs.bountyType || 'weekly' }}
            • <b>Past Week:</b> ${{ inputs.pastWeek || github.event.inputs.pastWeek || '0' }}
            • <b>Trigger:</b> ${{ github.event_name }}
            • <b>Status:</b> ${{ job.status }}
            📋 <a href="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}">View workflow logs</a>
